import React, { PureComponent } from 'react';
import PropTypes from 'prop-types';
import { hot } from 'react-hot-loader';

@hot(module)
export default class App extends PureComponent {
    render() {
        return <div>I am a login app</div>;
    }
}

import React, { Component } from 'react';
import PropTypes from 'prop-types';
import { hot } from 'react-hot-loader';

@hot(module)
export default class Polar extends Component {
    render() {
        return <div>Polar App</div>;
    }
}

import { updateTokens } from '../../utilities/tokens';

updateTokens();

// you need to look at chunks and code splitting for webpack assets
// as your results page code is interfering with your index page code.
